#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve() {
    int l,r;
    cin >> l >> r;
    vector<int> a(r-l+1);
    for(int& i:a) cin >> i;
}

int main()
{
    ll t;
    cin >> t;
    while (t--) solve();
    return 0;
}